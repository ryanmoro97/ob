

export default function queryVendorInsert(products, table) {
    // Excel -> Vendor

    // Map vendor fields to OB schema

    // Update prices on existing product in vendor table and OB

    // Insert non-existing products into selected table
}

