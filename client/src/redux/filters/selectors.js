export function getBrandFilter(state) {
    return state.filters.brandFilter;
}

export function getCategoryFilter(state) {
    return state.filters.categoryFilter;
}
  
export function getSubCategoryFilter(state) {
    return state.filters.subCategoryFilter;
}

export function getDescriptionFilter(state) {
    return state.filters.descriptionFilter;
}

export function getModelIdFilter(state) {
    return state.filters.modelIdFilter;
}

export function getSkuFilter(state) {
    return state.filters.skuFilter;
}

export function getBarcodeFilter(state) {
    return state.filters.barcodeFilter;
}

export function getPartNumFilter(state) {
    return state.filters.partNumFilter;
}

export function getModelYearFilter(state) {
    return state.filters.modelYearFilter;
}

