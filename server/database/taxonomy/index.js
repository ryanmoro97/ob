const taxonomy_brand = require('./taxonomy_brand');
const taxonomy_cat = require('./taxonomy_cat');
const taxonomy_sub_cat = require('./taxonomy_sub_cat');

module.exports = {
    taxonomy_brand,
    taxonomy_cat,
    taxonomy_sub_cat
};