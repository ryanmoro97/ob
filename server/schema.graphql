type Query {
  getTaxonomyValues(taxonomyClass: String!): [Taxonomy!]!
  getProductValues: [Product!]!
}

type Taxonomy {
  taxonomyId: Int!
  value: String!
}

type Product {
  productId: Int!
  name: String!
  description: String!
  price: Float!
}